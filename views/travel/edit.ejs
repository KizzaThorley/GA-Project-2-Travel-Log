<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1> edit page</h1>
    <form action="/travel/<%=user._id%>/edit/<%=locationId%>?_method=PUT" method="POST">
        <label for="country">Country:</label>
        <input type="text" name="country" id="country" value="<%=destination.country%>" required>
        <label for="place">Place:</label>
        <input type="text" name="place" id="place" value="<%=destination.place%>" required>
        <label for="hasBeen">Vistited</label>
        <input type="checkbox" name="hasBeen" id="hasBeen" <% if (destination.hasBeen) { %>checked>

            <label for="review">Review:</label>
            <textarea for="text" name="review" id="review"></textarea>
            
            <label for="rating">Rating:</label>
            <input type="text" name="rating" id="rating">
            <%}%>
        <button type="submit">Update Location</button>
        </form>
    
</body>
</html>